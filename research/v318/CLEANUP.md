# Archive cleanup

This archive is intentionally self-contained.

The inherited historical `src/` tree was removed. It contained source from
previous experiments that was not required to run V318.

The archive now contains only the files needed for V318 and its recorded
results.
